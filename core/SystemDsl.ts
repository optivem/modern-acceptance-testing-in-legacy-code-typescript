/**
 * Re-export from dsl/system to preserve existing imports (e.g. system-test).
 * The canonical implementation lives in dsl/system/SystemDsl.ts to match reference layout.
 */
export { SystemDsl } from '../dsl/system/SystemDsl.js';
