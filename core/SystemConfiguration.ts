/**
 * Re-export from dsl/system to preserve existing imports (e.g. system-test).
 * The canonical implementation lives in dsl/system/SystemConfiguration.ts to match reference layout.
 */
export { SystemConfiguration } from '../dsl/system/SystemConfiguration.js';
