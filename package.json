{
  "name": "modern-acceptance-testing-in-legacy-code-typescript",
  "version": "0.0.1",
  "description": "Modern Acceptance Testing in Legacy Code (TypeScript)",
  "private": true,
  "workspaces": [
    "monolith",
    "system-test"
  ],
  "scripts": {
    "install-all": "npm install && npm install --workspace=monolith && npm install --workspace=system-test",
    "build": "npm run build --workspace=monolith",
    "start": "npm start --workspace=monolith",
    "dev": "npm run dev --workspace=monolith",
    "test": "npm test --workspace=system-test"
  },
  "keywords": [
    "testing",
    "e2e",
    "acceptance-testing",
    "playwright",
    "typescript",
    "express"
  ],
  "author": "Optivem",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
