{
  "name": "modern-acceptance-testing-in-legacy-code-typescript",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "commons/*",
    "system-test"
  ],
  "scripts": {
    "build": "npm run build -w @optivem/util && npm run build -w @optivem/testing-dsl && npm run build -w @optivem/land && npm run build -w @optivem/http && npm run build -w @optivem/playwright && npm run build -w @optivem/testing-assertions",
    "test": "npm run build && npm run test -w system-test"
  }
}
